[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "nogil-rpc"
version = "0.1.0"
description = "A small Python-to-Python RPC runtime for free-threaded Python experiments."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools]
packages = ["nogil_rpc"]
